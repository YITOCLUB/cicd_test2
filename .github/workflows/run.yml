on:
  push:
    paths:
      - 'aws_TestB/**'
jobs:
  add_text:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |   
          echo "aws_TestB3333" >> hello.txt
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add .
          git commit -m "push"
          git push

